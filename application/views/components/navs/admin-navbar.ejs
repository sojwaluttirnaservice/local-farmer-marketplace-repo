<!-- Sidebar -->
<aside
    id="sidebar"
    class="w-[250px] md:shrink-0 min-h-screen p-6 bg-gradient-to-b from-gray-800 to-gray-900 text-white shadow-xl transform -translate-x-full transition-transform duration-300 ease-in-out z-50 lg:translate-x-0">
    <!-- Sidebar Header -->
    <div class="flex items-center justify-between mb-6">
        <h2 class="text-xl font-bold tracking-wide uppercase text-gray-100">Admin Panel</h2>
        <button onclick="" class="text-gray-400 text-2xl focus:outline-none lg:hidden">
            &times;
        </button>
    </div>

    <!-- Navigation -->
    <nav class="space-y-1">
        <a href="/admin/dashboard" class="sidebar-link"
            ><i class="fas fa-tachometer-alt text-blue-300"></i> Dashboard</a
        >
        <a href="/donors" class="sidebar-link"
            ><i class="fas fa-hand-holding-heart text-green-300"></i> Manage Donors</a
        >
        <a href="/recipients" class="sidebar-link"
            ><i class="fas fa-users text-yellow-300"></i> Manage Recipients</a
        >
        <a href="/donations" class="sidebar-link"
            ><i class="fas fa-gift text-purple-300"></i> Manage Donations</a
        >
        <a href="/requests" class="sidebar-link"
            ><i class="fas fa-hand-paper text-red-300"></i> Manage Requests</a
        >
        <a hidden href="/distributions" class="sidebar-link"
            ><i class="fas fa-truck text-pink-300"></i> Manage Distributions</a
        >

        <div class="p-[12px] mt-auto">
            <!-- Logout Button -->
            <button
                type="button"
                id="logout-btn"
                class="w-full px-4 py-2 text-red-400 border border-red-400 rounded-md">
                <i class="fas fa-sign-out-alt"></i> Logout
            </button>
        </div>
    </nav>
</aside>

<!-- Styles -->
<style>
    .sidebar-link {
        width: 100%;
        display: flex;
        align-items: center;
        gap: 10px;
        padding: 12px;
        border-radius: 8px;
        transition: all 0.3s ease-in-out;
        text-decoration: none;
        font-size: 14px;
        font-weight: 500;
        color: #fff;
    }

    .sidebar-link:hover {
        background: rgba(255, 255, 255, 0.1);
        backdrop-filter: blur(8px);
        box-shadow: 0 4px 6px rgba(255, 255, 255, 0.2);
    }
</style>
